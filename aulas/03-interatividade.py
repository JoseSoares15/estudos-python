# Agora vamos deixar o exemplo usado na aula anterior mais interativo, vamos pedir para o usuário digitar o nome, idade e peso dele e depois exibir na tela.

nome = input('Qual é o seu nome? ')
idade = input('Quantos anos você tem? ')
peso = input('Quanto você pesa? ')
print(nome, idade, peso)